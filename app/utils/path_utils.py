"""
Path Utils
==========
Path normalisation and workspace-relative conversion helpers.

Responsibilities:
    - Convert absolute paths to repo-relative paths
    - Normalise path separators to forward slashes
    - Resolve workspace root from configuration
    - Validate that file paths exist within workspace boundaries
"""
